{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "enabledManagers": [
    "custom.regex"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/^config\\/project\\.cfg$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)(?: packageName=(?<packageName>\\S+))?(?: versioning=(?<versioning>\\S+))?(?: extractVersion=(?<extractVersion>\\S+))?(?: registryUrl=(?<registryUrl>\\S+))?\\s+[A-Z0-9_]+\\s*=\\s*['\"](?<currentValue>[^'\"]+)['\"]"
      ],
      "versioningTemplate": "{{#if versioning}}{{{versioning}}}{{else}}semver{{/if}}"
    }
  ],
  "postUpgradeTasks": {
    "commands": [
      "sh scripts/update.sh config/project.cfg"
    ],
    "fileFilters": [
      "README.md",
      "config/project.cfg",
      "config/lockfile.cfg",
      "config/infra/Dockerfile",
      "config/infra/versions.tf"
    ],
    "executionMode": "branch"
  },
  "packageRules": [
    {
      "matchManagers": ["custom.regex"],
      "groupName": "project tooling and images"
    }
  ]
}
